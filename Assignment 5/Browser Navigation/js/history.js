//provides the current methods of interacting with the browsers history

// some methods 
//1 history.back-Loads the previous URL in the browser history
//2. history.forward -Loads the next URL in the browser history

function goBackward(){
    window.history.backward();
};

function Forward(){
    window.history.forward();
};